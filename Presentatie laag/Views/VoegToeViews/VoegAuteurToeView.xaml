<Window x:Class="Presentatie_laag.Views.VoegAuteurToeView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Presentatie_laag.Views"
        xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
        xmlns:cmd="http://www.galasoft.ch/mvvmlight"
        mc:Ignorable="d"
        Title="AuteurView" Height="80" Width="510"
        Name="VoegAuteurToeVieww"
        >
    
    <Grid Margin="5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0" Grid.Column="0">
            <Label Content="Naam"/>
            <TextBox Width="350" Height="20" Text="{Binding ToeTeVoegenAuteurNaam}"/>
            <Button Content="Voeg Toe" Margin="10,0,0,0" Width="80" Height="20" Command="{Binding VoegAuteurToeAanDbCommand}"/>
        </StackPanel>
    </Grid>
</Window>
