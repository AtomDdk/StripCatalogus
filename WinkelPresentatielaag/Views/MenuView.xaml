<Window x:Class="WinkelPresentatielaag.Views.MenuView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WinkelPresentatielaag.Views"
        mc:Ignorable="d"
        Title="MenuView" Height="200" Width="400">
    <Grid Margin="5">
        <Grid.RowDefinitions>
            <RowDefinition Height="0.5*"/>
            <RowDefinition Height="0.5*"/>
        </Grid.RowDefinitions>

        <Button Grid.Row="0" Content="Plaats levering" FontSize="15" Command="{Binding OpenMainViewAlsLeveringViewCommand}"/>
        <Button Grid.Row="1" Content="Plaats Bestelling" FontSize="15" Command="{Binding OpenMainViewAlsBestellingViewCommand}" Margin="0,5,0,0"/>

    </Grid>
</Window>
